{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild src/server.ts --bundle --outfile=dist/server.js --platform=node --tsconfig=tsconfig.json",
    "dev": "npm run build && npm run start",
    "start": "node dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@types/ws": "^8.5.14",
    "typescript": "^5.7.3",
    "ws": "^8.18.0"
  }
}
